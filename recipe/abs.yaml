# TEMPORARY: gviz-api is not yet published to pkgs/main; resolve it from the
# gviz-api-feedstock PR staging channel (AnacondaRecipes/gviz-api-feedstock#1).
# REMOVE this file before merge, once gviz-api is released to pkgs/main.
channels:
  - https://staging.continuum.io/pbp/fs/gviz-api-feedstock/pr1/f80ee69
